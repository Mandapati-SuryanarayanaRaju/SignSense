<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Gesture Result</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.0.0/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100">
    <!-- Navbar -->
    <nav class="p-4 bg-gray-900 border-b border-gray-400 shadow-md">
        <div class="flex items-center justify-between max-w-screen-xl px-16 mx-auto text-white">
            <h1 class="text-2xl font-semibold">
                <a href="/" class="flex items-center space-x-2 text-white">
                    <span class="text-blue-400">Sign</span><span class="text-yellow-400">Sense</span>
                </a>
            </h1>
            <div class="flex space-x-4">
                <a href="/" class="px-4 py-2 text-sm bg-blue-700 rounded hover:bg-blue-500">Home</a>
                <a href="/profile" class="px-4 py-2 text-sm bg-green-700 rounded hover:bg-green-500">Profile</a>
                <a href="/about" class="px-4 py-2 text-sm bg-yellow-700 rounded hover:bg-yellow-500">About</a>
                <a href="/config/" class="px-4 py-2 text-sm bg-purple-700 rounded hover:bg-purple-500">Config</a>
                <% if (user && user.role !== 'user') { %>  
                    <a href="/admin" class="px-4 py-2 text-sm bg-gray-700 rounded hover:bg-gray-500">Admin</a>
                <% } %>
                <a href="/logout" class="px-4 py-2 text-sm bg-red-700 rounded hover:bg-red-500">Logout</a>
            </div>
        </div>
    </nav>

  <section class="max-w-screen-xl px-6 py-8 mx-auto mt-10 mb-20 md:px-16">
    <div class="p-6 bg-white rounded-lg shadow-lg">
      <h1 class="mb-8 text-2xl font-semibold text-center">Detected Gesture</h1>
      
      <% if (imageUrl) { %>
        <div class="flex justify-center">
          <img src="<%= imageUrl %>" alt="Uploaded Gesture" class="w-64 h-64 rounded-lg shadow-lg">
        </div>
      <% } %>

      <% if (result) { %>
          <div class="mt-6 text-center">
              <h2 class="mb-4 text-lg font-semibold">Gesture Identified:</h2>
              <p class="text-xl font-bold text-blue-600"><%= result %></p>
              
              <% if (audioUrl) { %>
                <div class="mt-4">
                    <audio id="gestureAudio" src="<%= audioUrl %>" preload="auto"></audio>
                    <button 
                        onclick="document.getElementById('gestureAudio').play()" 
                        class="px-4 py-2 text-white bg-blue-500 rounded shadow hover:bg-blue-700">
                        ▶ Play Gesture Sound
                    </button>
                </div>
              <% } %>
          </div>
      <% } else { %>
          <p class="font-semibold text-center text-red-500">No gesture detected.</p>
      <% } %>    
    </div>

    <a href="/" class="block w-full p-4 mx-auto my-6 text-center text-white duration-300 bg-pink-700 rounded shadow hover:bg-pink-600">Back To Home</a>
  </section>

  <footer class="fixed bottom-0 w-full p-4 text-center text-white bg-gray-900 border-t border-gray-400 shadow-md mt-28">
    &#169; SignSense
  </footer>
</body>
</html>